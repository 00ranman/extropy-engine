{
  "name": "@extropy/extropialingo",
  "version": "1.0.0",
  "description": "Revolutionary gamified Extropian language learning with physics-based XP rewards and entropy reduction tracking",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "test": "jest",
    "build:wip": "tsc"
  },
  "dependencies": {
    "@extropy/xp-formula": "*",
    "express": "^4.18.0"
  },
  "license": "MIT",
  "//build": "WIP: package has unresolved pre-existing errors (missing service files, ioredis/DOM type issues). Run 'pnpm run build:wip' to attempt build."
}
