{
  "name": "@extropy/github-parasite",
  "version": "0.1.0",
  "description": "GitHub App parasite: listens to PR merges on host repos, computes ΔS-in-code claims, mints XP tied to contribution ratio (never market position). Non-extraction invariant enforced end-to-end.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@extropy/contracts": "*",
    "@extropy/xp-formula": "*",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "vitest": "^2.0.0"
  }
}
