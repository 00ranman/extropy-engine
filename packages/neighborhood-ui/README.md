# Neighborhood UI

The simple face: job board, MICRO crews, DAG you can audit.

**Live (browser book, start today):** https://extropyengine.com/hoa/board

On a node, the same loops hit loop-ledger / xp-mint. The website board is the face so a neighbor can log a mow before Docker finishes building. Export the DAG JSON and anyone can review it.

Sunset Oaks MESO: `presets/sunset-oaks`.
