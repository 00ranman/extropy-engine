{
  "name": "@extropy/epistemology-engine",
  "version": "0.1.0",
  "description": "Mesh observability layer for the emergent peer-review system. Witnesses, aggregates, and surfaces the epistemology that arises from incentive-aligned validation across the network. Redefined in v3.1 — see README.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@extropy/bayesian": "workspace:*",
    "@extropy/contracts": "*",
    "@extropy/decomposition-kit": "workspace:*",
    "express": "^4.19.0",
    "ioredis": "^5.3.0",
    "pg": "^8.12.0",
    "uuid": "^10.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/pg": "^8.11.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "vitest": "^2.0.0"
  }
}
