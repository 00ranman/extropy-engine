# EDGE ONLY — not the architecture center

`homeflow` is a **product edge**. Extropy Engine is **meter-first**:

**CT · EP · L · CAT · IT** (+ XP mint)

**Identity:** `POST /auth/session` with the **DID your own node mints**.
Google Auth / OAuth / KYC are **removed**. No central customer registry.

See [`ARCHITECTURE.md`](../../ARCHITECTURE.md), [`docs/architecture/IDENTITY.md`](../../docs/architecture/IDENTITY.md), [`DIAGRAM.md`](../../DIAGRAM.md).

Do not use this package as the lead for architecture diagrams.
